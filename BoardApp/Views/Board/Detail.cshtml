@model  BoardApp.Models.Board


@{
    ViewBag.Title = "상세페이지";
    Layout = "~/Views/Shared/_t_sub_Layout.cshtml";
}

<section>
    <article>

        <div class="row">
            <div class="col-lg-12">
                <h3 class="page-header" id="detail-title">@Model.BoardTitle</h3>
            </div>

            <div class="col-lg-12" id="detail-panel-details">
                <span>작성자: @Model.BoardWriter </span><span>  |  </span><span>작성일: @Model.CreatedDate</span><span>  |  </span><span>조회수: @Model.ViewCount</span>
            </div>

            <div class="col-lg-12">

                <div class="panel panel-default">
                    <div class="panel-body" id="detail-panel-contents">@Model.BoardContent</div>
                    <div class="panel-footer text-right">
                        <a class="btn btn-default" href="/Board/Update?BoardNo=@Model.BoardNo" />수정</a>
                        @*<a class="btn btn-warning" id="boardDelete-button" href="/Board/Delete?BoardNo=@Model.BoardNo">삭제</a>*@

                        <a class="btn btn-warning" id="boardDelete-button" data-no="@Model.BoardNo" @*href="/Board/Delete?BoardNo=@Model.BoardNo"*@>삭제</a>

                        <a class="btn btn-primary" href="/Board/Index">목록</a>
                    </div>
                </div>
            </div>

            @*<div class="row">
                    <div class="col-lg-12">
                        <div class="panel panel-default">
                            <div class="panel-heading">
                                @Model.BoardTitle
                            </div>
                            <div class="panel-body">
                                @Model.BoardContent
                            </div>
                        </div>
                        <div class="panel-footer">
                            <a class="btn btn-primary" href="/Board/Index">목록</a>
                        </div>
                    </div>
                </div>*@


        </div> <!--게시글 내용 end-->
        <!--댓글-->

        <div class="row">
            <div class="col-lg-12">
                <span class="glyphicon glyphicon-comment" aria-hidden="true"></span>
                <span class="commment-count">댓글 @Model.CommentCount 개</span>
            </div>
        </div>


        <div class="comment-view">

            <!--레벨1-->
            @*<div class="row comment-listRow" comment-no="{{CommentNo}}" board-no="{{BoardNo}}" comment-level="{{CommentLevel}}" comment-order="{{CommentOrder}}" flag="{{CommentFlag}}">
                    <div class="col-lg-12">
                        <span class="comment-writer">{{CommentWriter}}</span>
                        <span class="comment-createdDate">{{CreatedDateString}}</span>
                        <span class="comment-reply-button"><a href="#">답글</a></span>
                        <p class="comment-edit-button"><a href="#">수정</a><span> | </span><a href="#">삭제</a></p>
                    </div>
                    <div class="col-lg-12">
                        <p class="comment-body"><span class="comment-content">{{CommentContent}}</span></p>
                    </div>
                </div>*@

            <!--레벨2-->
            @*<div class="row comment-listRow indent-right" comment-no="{{CommentNo}}" board-no="{{BoardNo}}" comment-level="{{CommentLevel}}" comment-order="{{CommentOrder}}" flag="{{CommentFlag}}">
                    <div class="col-lg-12">
                        <span class="comment-writer">{{CommentWriter}}</span>
                        <span class="comment-createdDate">{{CreatedDateString}}</span>
                        <span class="comment-reply-button"><a href="#">답글</a></span>
                        <p class="comment-edit-button"><a href="#">수정</a><span> | </span><a href="#">삭제</a></p>
                    </div>
                    <div class="col-lg-12">
                        <p class="comment-body"><span class="comment-content">{{CommentContent}}</span></p>
                    </div>
                </div>*@

        </div>

            <div class="panel panel-default">
                <div class="panel-body">


                    <form>
                        <div class="form-row">
                            <div class="form-group col-md-6" id="commentInputBox_writer">
                                <label for="inputEmail4">Name</label>
                                <input type="email" class="form-control" id="comment-inputWriter" maxlength="5" placeholder="작성자">
                            </div>
                            <div class="form-group col-md-6" id="commentInputBox_password">
                                <label for="inputPassword4">Password</label>
                                <input type="password" class="form-control" id="inputPassword4" placeholder="Password">
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="inputAddress">Comment</label>
                            <textarea type="text" class="form-control" id="comment-inputContent" placeholder="댓글을 입력하세요."></textarea>
                        </div>

                        <button type="submit" class="btn btn-primary" id="comment-add-btn">등록</button>
                        
                    </form>





                </div> <!--panelbody-->
            </div>


    </article>
</section>


<!--Handlebars 템플릿-->
<script id="comment-template" type="text/x-handlebars-template">
    {{#each commentList}}
    <div class="row comment-listRow" 
         comment-no="{{CommentNo}}" 
         board-no="{{BoardNo}}" 
         origin-commentNo="{{OriginCommentNo}}"
         comment-level="{{CommentLevel}}" comment-order="{{CommentOrder}}" flag="{{CommentFlag}}">

        <div class="col-lg-12">
            <span class="comment-writer">{{CommentWriter}}</span>
            <span class="comment-createdDate"> {{CreatedDateString}}</span>
            <span class="comment-reply-button"><a class="comment-replyBtn" href="#">답글</a></span>
            <p class="comment-edit-button"><a href="#">수정</a><span> | </span><a href="#">삭제</a></p>
        </div>

        <div class="col-lg-12">
            <span class="comment-parentCommentWriter" parent-commentWriter="{{ParentCommentWriter}}">&commat;{{ParentCommentWriter}}</span>
            <p class="comment-body"><span class="comment-content">{{CommentContent}}</span></p>
        </div>

    </div>
    {{/each}}
</script>

<script id="comment-editor-template" type="text/html">
    <div class="panel panel-default" id="reCommentForm">
        <div class="panel-body">
            <form>
                <div class="form-row">
                    <div class="form-group col-md-6" id="commentInputBox_writer">
                        <label for="inputEmail4">Name</label>
                        <input type="email" class="form-control" id="comment-inputWriter" maxlength="5" placeholder="작성자">
                    </div>
                    <div class="form-group col-md-6" id="commentInputBox_password">
                        <label for="inputPassword4">Password</label>
                        <input type="password" class="form-control" id="inputPassword4" placeholder="Password">
                    </div>
                </div>
                <div class="form-group">
                    <label for="inputAddress">Comment</label>
                    <textarea type="text" class="form-control" id="comment-inputContent" placeholder="댓글을 입력하세요."></textarea>
                </div>

                <button type="submit" class="btn btn-primary" id="comment-add-btn">취소</button>
                <button type="submit" class="btn btn-primary" id="comment-add-btn">등록</button>

            </form>
            </div>
        </div>
</script>


